name: CI

on: [push]

jobs:
  pipeline:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: make build
    - name: self-include
      run: make self-include
    - name: commitlint
      run: make commitlint MKDKR_COMMITLINT_TAG=$MKDKR_BRANCH_NAME_SLUG
    - name: push
      run: make push
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
